/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["img.sakneen.com", "img.sakneen.com/placeholder"],
  },
};

module.exports = nextConfig;
